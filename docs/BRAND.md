# Lantern — product brand

**Product name:** Lantern  
**Registry / packages:** unchanged (`web`, `templates`, `web_app`, …)  
**Language:** Ori  

## Tagline (canonical)

**EN:** Ori is my light. Lantern is how it shines on the web.  

**pt-BR:** Ori é a minha luz. Lantern é como ela chega à web.  

## Positioning

| Layer | Name |
|-------|------|
| Language | Ori (*ori* — “minha luz”) |
| Package registry | OriLamp |
| HTML-first web stack | **Lantern** |

## Product one-liner (secondary)

HTML-first web stack for Ori: readable code, readable pages, one native binary.

## Do not

- Rename package IDs (`web`, `templates`) without a major version plan  
- Use “OriLantern” as the product name — brand is **Lantern** alone  
