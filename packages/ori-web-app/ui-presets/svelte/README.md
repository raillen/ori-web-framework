# Svelte preset

```bash
cd frontend
npm install
npm run dev    # HMR + proxy /api → Ori :3000
npm run build  # → ../public
```

Ori serves `public/` via `web.static`. Core framework never requires Node.
