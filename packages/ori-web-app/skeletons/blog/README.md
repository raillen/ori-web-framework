# {{APP_NAME}} (blog)

Blog skeleton: home + in-memory posts. Scaffolded with `tools/ori-web new … --template=blog`.

```bash
ori get .
ori run main.orl
```

Add resources: `../../tools/ori-web generate scaffold comments` (from app root, adjust path).
